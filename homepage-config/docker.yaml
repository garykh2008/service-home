---
# Docker 連線設定
# 文件：https://gethomepage.dev/configs/docker/
#
# 讀取 docker-compose.yml 唯讀掛載的 /var/run/docker.sock，
# 用來在服務卡片上顯示容器狀態（上/下線）與 CPU / 記憶體摘要。
# 邊界：僅「一眼看有沒有掛掉」的摘要，重啟容器 / 看 log 仍走 VPS Dashboard。

my-docker:
  socket: /var/run/docker.sock
