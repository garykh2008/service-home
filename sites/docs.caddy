docs.garyhsieh-proj.com {
	# 登入保護（basic_auth）。密碼 hash 從環境變數 DOCS_AUTH_HASH 帶入，不進版控。
	# 設定方式見 README「docs 頁面登入」（跟 home/report 同一套做法，各自獨立帳密）。
	basic_auth {
		gary {$DOCS_AUTH_HASH}
	}

	# 反代到 APIDocGenerator（/root/APIDocGenerator，只綁 127.0.0.1:8090）。
	reverse_proxy 127.0.0.1:8090
}
